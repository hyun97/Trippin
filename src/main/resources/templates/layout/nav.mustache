<div class="navbar-fixed">
    <nav>
        <div class="nav-wrapper container">
            <!-- Logo -->
            <a id="logo-container" href="/" class="brand-logo">Trippin</a>

            <form class="hide-on-med-and-down">
                <div class="input-field">
                    <input id="search" type="search" class="white" required>
                    <label class="label-icon" for="search"><i class="material-icons black-text">search</i></label>
                    <i class="material-icons">close</i>
                </div>
            </form>

            <!-- Links -->
            <ul class="right hide-on-med-and-down">
                {{#isLogin}}
                    <li><a class="btn">팔로잉</a></li>
                    <li>
                        <a href="/user/{{loginUser.id}}" class="modal-trigger">
                            <i class="material-icons">person</i>
                        </a>
                    </li>
                    <li>
                        <a href="/logout" class="modal-trigger">
                            <i class="material-icons">power_settings_new</i>
                        </a>
                    </li>
                {{/isLogin}}
                {{^isLogin}}
                    <li><a href="#contact" class="btn modal-trigger">로그인</a></li>
                {{/isLogin}}
                <!-- Modal -->
                <div id="contact" class="form-structor modal">
                    <div class="signup modal-content">
                        <h2 class="form-title" id="signup">Sign up</h2>
                        <button class="submit-btn" onclick="location.href='/oauth2/authorization/google'">
                            GOOGLE
                        </button>
                        <button class="submit-btn">NAVER</button>
                    </div>
                </div>
            </ul>

            <!-- Side nav trigger (On Mobile) -->
            <a href="#" data-target="mobile-links" class="sidenav-trigger left"><i
                    class="material-icons">menu</i></a>
        </div>
    </nav>
</div>

<!-- Side nav (On Mobile) -->
<ul id="mobile-links" class="sidenav">
    {{#user}}
        <li>
            <div class="user-view">
                <div class="background">
                    <img src="{{user.picture}}">
                </div>
                <a href=""><img class="circle" src="{{user.picture}}"></a>
                <a href=""><span class="white-text name">{{user.name}}</span></a>
                <a href=""><span class="white-text email">{{user.email}}</span></a>
            </div>
        </li>
        <li><a href=""><i class="material-icons">home</i>홈</a></li>
        <li><a href=""><i class="material-icons">person</i>마이페이지</a></li>
        <li><a href=""><i class="material-icons">favorite</i>팔로잉</a></li>
    {{/user}}
    {{^user}}
        <li><a href=""><i class="material-icons">forward</i>로그인</a></li>
        <li><a href=""><i class="material-icons">home</i>홈</a></li>
    {{/user}}
    <li>
        <div class="divider"></div>
    </li>
    <li><a class="subheader">Subheader</a></li>
    <li><a class="waves-effect" href="#">Third Link With Waves</a></li>
</ul>
