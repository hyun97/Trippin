{{>layout/header}}

<div class="row container follow-wrapper">
    <h2>팔로잉</h2>
    {{#followingUser}}
        <a href="/user/{{id}}">
            <div class="follow-card col s12 m6 l4 xl2">
                <div class="our-team">
                    <div class="picture">
                        {{#validFollowing}}
                            <img class="img-fluid active-follow" src="{{picture}}">
                        {{/validFollowing}}
                        {{^validFollowing}}
                            <img class="img-fluid" src="{{picture}}">
                        {{/validFollowing}}
                    </div>
                    <div class="team-content">
                        <h5 class="name">{{name}}</h5>
                    </div>
                    {{#validFollowing}}
                        <ul class="social">
                            <li>
                                <span class="white-text">맞팔중 😆</span>
                            </li>
                        </ul>
                    {{/validFollowing}}
                </div>
            </div>
        </a>
    {{/followingUser}}
</div>

{{>layout/footer}}
